# Wiki 维护规则

这个 vault 是一个由 LLM 维护的研究 wiki，主题是“基于代码自进化的机器人技能库系统”。核心目标不是保存聊天记录，而是把来源、论文和思考逐步编译成可追溯、可更新、互相链接的中文知识库。

## 三层结构

1. `Source/` 是原始来源层。这里保存用户想法、论文清单、文章摘录和后续加入的原始资料。除非用户要求整理来源元数据，不要改写原始材料的观点。
2. `Wiki/` 是 LLM 生成的知识层。这里放主题页、概念页、相关工作页、研究判断页。LLM 可以创建、合并和更新这些页面。
3. `index.md` 与 `log.md` 是导航层。`index.md` 按内容列出页面；`log.md` 按时间记录重要维护动作。

## 当前目录约定

- `Wiki/00-总览/`：项目入口和总览。
- `Wiki/01-核心概念/`：架构、概念、机制。
- `Wiki/02-相关工作/`：论文地图和技术路线。
- `Wiki/02-相关工作/论文精读/`：逐篇论文的 PhD advisor 风格技术讲解。
- `Wiki/03-研究判断/`：可行性、边界、实验路线、开放问题。
- `Source/Literature/资料与论文清单.md`：已收集资料的主清单。

不要过早新增目录。只有当某类页面达到 5 个以上，并且已经影响导航时，再考虑拆新目录。

## 页面写作原则

- 用中文自然语言写，优先解释“这对本项目意味着什么”。
- 每个页面开头先给一句核心判断，再展开。
- 重要事实必须给来源链接；没有来源的判断要标明是“推断”或“研究假设”。
- Wiki 页面应尽量图文并茂：在适合的位置加入论文图示、系统架构图、流程图、对比表或 Mermaid 图，帮助读者快速把握机制、层次关系和研究判断；图片或图表必须服务论点，不做装饰性堆砌。
- 尽量用 Obsidian 双链连接稳定概念，例如 `[[核心架构]]`、`[[VLA路线]]`。
- 一个概念反复出现 3 次以上，或者未来会被持续更新，才单独建页。
- 不为了完整而完整。这个 vault 的价值是形成可执行研究判断，不是复刻论文数据库。

## 更新工作流

### 加入新来源

1. 把来源放入 `Source/`，或在 `Source/Literature/资料与论文清单.md` 追加条目。
2. 判断它影响哪些已有 wiki 页面。
3. 更新相关页面中的论点、证据和局限。
4. 更新 `index.md`。
5. 在 `log.md` 追加一条记录。

### 加入新论文

按 [[论文入库工作流]] 执行。默认流程是：把 PDF 放入 `Source/Papers/`，抽取摘要和章节结构，在 `Wiki/02-相关工作/论文精读/` 新建逐篇精读页，然后更新对应主题页、`index.md`、资料清单和 `log.md`。

### 回答研究问题

1. 先读 `index.md` 找相关页。
2. 再读相关 wiki 页和必要的 Source。
3. 回答时区分：已有证据、合理推断、尚未验证。
4. 如果回答形成了稳定的新分析，应写回 `Wiki/`，而不是只留在聊天里。

### 健康检查

周期性检查：

- 是否有页面互相矛盾。
- 是否有新论文推翻旧判断。
- 是否有孤立页面缺少链接。
- 是否有重要概念只在多个页面被提到、但没有自己的定义。
- 是否有实验路线缺少可测指标。

## 机器人研究的特别约束

- 不把“通用 AGI”作为直接可验证目标。优先表述为“执行反馈驱动的机器人 skill 自调试与自进化”。
- 区分高层规划、skill 调用、低层控制、程序修复、安全验证这几个层次。
- 对真实机器人部署保持保守：任何 coding agent 修改都必须经过静态检查、仿真、回归测试、安全门和回滚机制。
- VLA、传统控制、机器人 DSL、coding agent 不是互斥路线。默认研究方向是混合系统。
